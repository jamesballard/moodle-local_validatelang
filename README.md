moodle-local_validatelang
=========================

Prints all language strings from a given component on one screen to validate HTML and run extra checks for case (e.g. sentence case as used by most of Moodle).
